- name: Update apt cache
  apt:
    update_cache: yes

- name: Install common packages
  apt:
    name:
      - curl
      - unzip
      - gnupg
      - ca-certificates
    state: present
